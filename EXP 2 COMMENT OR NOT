#include<stdio.h>
#include<string.h>
int main()
{
	char s[100],length,i;
	printf("Enter the text:\n");
	scanf("%s",s);
	int len=strlen(s);
	if(s[0]=='/' && s[1]=='/' || s[len-1]=='*' && s[len-2]=='/')
	printf("It is comment.");
	else
	printf("It is not a comment");
	return 0;
}     
